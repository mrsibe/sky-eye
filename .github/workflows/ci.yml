name: CI

on:
  push:
    branches: ['**']
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend quality
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.1.3
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Check formatting
        run: pnpm format:check
      - name: Lint
        run: pnpm lint
      - name: Test frontend cache and WCS transforms
        run: pnpm test
      - name: Check application versions
        run: pnpm version:check
      - name: Type-check and build frontend
        run: pnpm build

  rust:
    name: Rust quality
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Check Rust formatting
        run: cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check
      - name: Clippy
        run: cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
      - name: Test Rust
        run: cargo test --manifest-path src-tauri/Cargo.toml --all-targets
      - name: Validate fixed ADES PSV with official tools
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install "iau-ades @ git+https://github.com/IAU-ADES/ADES-Master.git@39ae5a9b13c37f7256a39b457e04433c854ed4dd"
          psvtoxml.py src-tauri/tests/golden/ades-fixed.psv /tmp/ades-fixed.xml
          valsubmit.py /tmp/ades-fixed.xml
          grep -Fx 'submit is OK' valsubmit.file
